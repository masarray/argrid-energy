# Security policy

ArGrid is currently a static demonstration and does not accept field-device commands or store credentials.

Do not commit passwords, API keys, certificates, private meter data, network diagrams from real facilities, or customer-identifying information.

For a future production deployment, use a secured backend integration layer, authenticated APIs, role-based access control, audit logging, encrypted transport, network segmentation, and independent cybersecurity review.
